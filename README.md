# project1

This folder contains scripts for the Big Data project (PNJtoJSON, MongoDBToNeo4j, etc.).

Contents:
- `PNJtoJSON.py` — convert PNJ to JSON (user-provided script)
- `MongoDBToNeo4j.py` — (existing script)

How to push to GitHub:
1. Create a repository on GitHub (https://github.com/new).
2. Add the remote and push:
   - `git remote add origin <REMOTE_URL>`
   - `git push -u origin main`

If you want, provide the remote URL and I can add it and push for you.
